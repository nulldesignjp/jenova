import * as THREE from 'three';
import Jenova from '../libs/jenova';

import gsap from 'gsap';
import { useEffect, useRef } from 'react';

export default class BasicScene extends Jenova.Scene
{
    constructor()
    {
        super()
    }

    init(){}

}